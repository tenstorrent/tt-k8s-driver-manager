apiVersion: v2
name: tt-k8s-driver-manager
description: Manages the host-side software lifecycle for Tenstorrent hardware — tt-kmd kernel module, device firmware, and tt-smi CLI — via TenstorrentDriverPolicy and TenstorrentFirmwarePolicy CRDs.
type: application
version: 0.1.0
appVersion: "0.1.0"
